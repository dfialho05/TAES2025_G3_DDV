# ============================================
# CONFIGURAÇÃO DO SERVIDOR WEBSOCKET
# ============================================

# ID único deste servidor (útil para ambientes multi-node)
SERVER_ID=websocket-server-1

# Porta do servidor WebSocket
WEBSOCKET_PORT=3000

# ============================================
# CONFIGURAÇÃO DO REDIS
# ============================================

# Host do Redis
REDIS_HOST=127.0.0.1

# Porta do Redis
REDIS_PORT=6379

# Password do Redis (deixar vazio se não tiver autenticação)
REDIS_PASSWORD=

# Database do Redis (0-15)
REDIS_DB=0

# ============================================
# CONFIGURAÇÃO DO WATCHDOG
# ============================================

# Intervalo de verificação do Watchdog em milissegundos (padrão: 30000 = 30s)
WATCHDOG_INTERVAL=30000

# Timeout do jogo em milissegundos - tempo sem heartbeat para considerar timeout (padrão: 120000 = 2min)
GAME_TIMEOUT_MS=120000

# Duração máxima de um jogo em milissegundos (padrão: 3600000 = 1h)
MAX_GAME_DURATION_MS=3600000

# TTL do heartbeat em segundos (padrão: 180 = 3min)
HEARTBEAT_TTL=180

# ============================================
# CONFIGURAÇÃO DA API LARAVEL
# ============================================

# URL base da API Laravel
LARAVEL_API_URL=http://127.0.0.1:8000/api

# Timeout para chamadas à API em milissegundos
API_TIMEOUT=5000

# ============================================
# CONFIGURAÇÃO DE SINCRONIZAÇÃO
# ============================================

# Intervalo de sincronização periódica para Redis em milissegundos (padrão: 10000 = 10s)
SYNC_INTERVAL=10000

# TTL padrão para jogos no Redis em segundos (padrão: 7200 = 2h)
DEFAULT_GAME_TTL=7200

# ============================================
# CONFIGURAÇÃO DE LOGGING
# ============================================

# Nível de logging (debug, info, warn, error)
LOG_LEVEL=info

# Ativar logs de debug do Redis (true/false)
REDIS_DEBUG=false

# ============================================
# NOTAS
# ============================================
#
# 1. Copie este ficheiro para .env e ajuste os valores conforme necessário
# 2. Certifique-se de que o Redis está instalado e rodando antes de iniciar o servidor
# 3. Para instalar o Redis:
#    - Ubuntu/Debian: sudo apt-get install redis-server
#    - MacOS: brew install redis
#    - Windows: https://github.com/microsoftarchive/redis/releases
# 4. Para verificar se o Redis está rodando: redis-cli ping (deve retornar PONG)
# 5. Para ambientes de produção, configure senha para o Redis
